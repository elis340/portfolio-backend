# Core dependencies
setuptools>=65.0.0  # Provides distutils compatibility for Python 3.12+
pandas>=2.0.0,<3.0.0  # Pin to 2.x for pandas-datareader compatibility (pandas 3.0 not yet supported)
numpy>=1.24.0
matplotlib>=3.7.0
yfinance>=0.2.40
scipy>=1.10.0
pandas-datareader>=0.10.0
statsmodels>=0.14.0

# API server dependencies (optional - only needed if running API server)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
